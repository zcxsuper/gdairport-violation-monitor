# ============================================
# RAG-GDGS Docker Compose 环境变量配置模板
# ============================================
# 说明：
# 1. 复制此文件为 .env：cp .env.example .env
# 2. 修改 .env 文件中的配置值
# 3. 修改后重新启动服务：docker-compose down && docker-compose up -d
# 4. .env 文件已在 .gitignore 中排除，不会提交到代码仓库
# ============================================

# ============================================
# 基础配置
# ============================================
# 时区配置（所有服务共用）
TZ=Asia/Shanghai

# ============================================
# 存储路径配置
# ============================================
# 所有数据存储的根路径前缀
# 默认值：/data/gdairport-violation-monitor
# 建议：生产环境使用独立数据盘，如 /data-disk/gdairport-violation-monitor
STORAGE_PATH=./data-disk/gdairport-violation-monitor

# ============================================
# MySQL 配置
# ============================================
# MySQL root 用户密码
# ⚠️ 生产环境必须修改为强密码
MYSQL_ROOT_PASSWORD=root

# MySQL 数据库名称
MYSQL_DATABASE=rag_gdgs

# ============================================
# RabbitMQ 配置（容器服务）
# ============================================
# RabbitMQ 默认用户名
RABBITMQ_DEFAULT_USER=rabbitmq

# RabbitMQ 默认密码
# ⚠️ 生产环境建议修改为强密码
RABBITMQ_DEFAULT_PASS=rabbitmq

# RabbitMQ 虚拟主机
RABBITMQ_DEFAULT_VHOST=/gdairport-violation-monitor

# ============================================
# 应用服务配置
# ============================================
# Spring Boot Profile
# 可选值：dev, pro
SPRING_PROFILES_ACTIVE=dev

# ============================================
# minIO 配置
# ============================================

MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
